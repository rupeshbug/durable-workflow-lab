version: "3.9"

services:
  redis:
    image: redis:8-alpine
    container_name: workflow-lab-redis

    ports:
      - "6379:6379"

    command: redis-server --appendonly yes

    volumes:
      - redis-data:/data

volumes:
  redis-data: